type            runTimeControl;
libs            (utilityFunctionObjects);

conditions
{
    forces
    {
        type            average;
        functionObject  forceCoeffs;
        fields          (Cd Cl);
        tolerance       1e-5;
        window          100;
        windowType      exact;
    }
}

satisfiedAction end;
